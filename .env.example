# Bu dosyayı ".env" olarak kopyalayıp gerçek değerleri gir.
# .env dosyasını ASLA GitHub'a yükleme (.gitignore'da olmalı).

SUPABASE_URL=
SUPABASE_KEY=

# Supabase ayarlanmazsa uygulama otomatik olarak lokal SQLite'a düşer.
# REPAIR_DB_PATH=data/daily_repair_rate.sqlite
